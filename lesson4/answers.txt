1. Как получить в виде результата все поля из таблицы employee?
select * form employee;

2. Как получить в виде результата только поля name_first, name_last, salary из таблицы employee?
select name_first, name_last, salary from employee;

3. Задать таблице employee псевдоним e и вывести всех, у кого salary выше 3800.
select * from employee e where e.salary>3800;

4. Так выбрать страны, из которых поставляют продукцию производители, чтобы страны не повторялись по 2 и более раз.
SELECT DISTINCT name FROM provider;

5. Вывести все автобусы в порядке уменьшения количества мест.
SELECT * FROM repetition.buses ORDER BY seat_capacity;

6. Перечислить известные агрегирующие функции в MySQL.
max, min, count, avg, sum

7. Есть таблица с полями name и id. Нужно вывести имя с наибольшим id, не используя при этом команду MAX. Как это можно сделать?
SELECT name FROM tab ORDER BY id DESC LIMIT 1;

8. Обязательно ли писать команды прописными буквами? Сработает ли запрос, если его написать строчными буквами?
ДА. но обычно зарезервированые слова пишут большими буквами, а имета таблиц, полей - маленькими.
так гораздо проще воспринимается запрос.

9. Что такое SQL-инъекции и как их избегать?
когда через web-форму передаются данные, которые без проверки (или с недостаточной проверкой) вставляются в шаблон запроса и позволяют выполнить произвольные и не предусмотренные разработчиком запросы в БД.
чтобы не допустить инъекций, необходимо проверять все параметры, которые передает пользователь на наличие различных спец.символов.

p.s. создавать файл для проверки таких простых запросов как то лень. сори.
